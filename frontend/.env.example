# ── 前端環境變數範本 ──
# 本機開發通常「不需要」任何設定：
# Vite 開發伺服器會自動把 /api 代理到 http://127.0.0.1:8000（見 vite.config.ts）。
#
# 部署時若不使用反向代理（vercel.json 的 rewrites），可改用絕對網址：
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
